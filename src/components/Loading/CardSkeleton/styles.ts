import { motion } from 'framer-motion';
import styled from 'styled-components';

export const SkeletonItem = styled(motion.div)`
  background-color: var(--blue);
  border-radius: 4px;
  margin-bottom: 10px;
`;
